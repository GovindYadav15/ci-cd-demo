version: '3.8'
services:
  app:
    image: ${IMAGE_NAME:-ci-cd-demo}:${TAG:-latest}
    ports:
      - "4173:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
    restart: unless-stopped
